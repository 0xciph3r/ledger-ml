apiVersion: v1
kind: Service
metadata:
  name: ledger-ml-webhook-service
  namespace: system
  labels:
    app.kubernetes.io/name: ledger-ml
    app.kubernetes.io/component: webhook
spec:
  selector:
    app.kubernetes.io/name: ledger-ml
    app.kubernetes.io/component: controller
  ports:
    - name: https
      port: 443
      targetPort: 9443
