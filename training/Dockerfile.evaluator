FROM python:3.12-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY ledgerml_training ./ledgerml_training

ENTRYPOINT ["python", "-m", "ledgerml_training.quality_gate"]
